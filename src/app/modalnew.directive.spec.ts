import { ModalnewDirective } from './modalnew.directive';

describe('ModalnewDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalnewDirective();
    expect(directive).toBeTruthy();
  });
});
